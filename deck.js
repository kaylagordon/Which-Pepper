class Deck {
  constructor(deckObj) {
    this.cards = deckObj.cards;
    this.matchedCards = [];
    this.selectedCards = [];
    this.matches = 0;
  }

  shuffle() {

  }

  checkSelectedCards() {
    if(this.cards[i].selected = true) {
      counter++;
    }
  }

  moveToMatched() {

  }

};
